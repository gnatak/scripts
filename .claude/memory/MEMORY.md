<!-- Project-local memory index. Versioned in git so it syncs between dev machines.
     Future memories: write each as its own file in this directory and add a one-line
     pointer below. Format: `- [Title](file.md) — one-line hook` -->
